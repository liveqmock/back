package com.ihk.sale.data.pojo;

import java.util.Date;
import java.math.BigDecimal;
import java.io.Serializable;

/**
 * SaleBuildLog的实体类
 * @author 
 *
 */
public class SaleBuildLog implements Serializable{
	private static final long serialVersionUID = 1L;

	//以下由代码生成器生成,如果手工添加代码，务必写在本代码页面最下方
    
	private int id;
	private Date fromDate;
	private Date toDate;
	private Date buildTime;
	private String isSucess;

	/**
	 * 取得Id()
	 */
	public int getId() {
		return id;
	}

	/**
	 * 设置id()
	 * @param id ()
	 */
	public void setId(int id) {
		this.id = id;
	}
    
	/**
	 * 取得FromDate(开始日期)
	 */
	public Date getFromDate() {
		return fromDate;
	}

	/**
	 * 设置fromDate(开始日期)
	 * @param fromDate (开始日期)
	 */
	public void setFromDate(Date fromDate) {
		this.fromDate = fromDate;
	}
    
	/**
	 * 取得ToDate(结束日期)
	 */
	public Date getToDate() {
		return toDate;
	}

	/**
	 * 设置toDate(结束日期)
	 * @param toDate (结束日期)
	 */
	public void setToDate(Date toDate) {
		this.toDate = toDate;
	}
    
	/**
	 * 取得BuildTime(截数日期时间)
	 */
	public Date getBuildTime() {
		return buildTime;
	}

	/**
	 * 设置buildTime(截数日期时间)
	 * @param buildTime (截数日期时间)
	 */
	public void setBuildTime(Date buildTime) {
		this.buildTime = buildTime;
	}
    
	/**
	 * 取得IsSucess(是否成功)
	 */
	public String getIsSucess() {
		return isSucess;
	}

	/**
	 * 设置isSucess(是否成功)
	 * @param isSucess (是否成功)
	 */
	public void setIsSucess(String isSucess) {
		this.isSucess = isSucess;
	}
    
	
	public SaleBuildLog(){};


	/**
	 * 
	 * @param id ()
	 * @param fromDate (开始日期)
	 * @param toDate (结束日期)
	 * @param buildTime (截数日期时间)
	 * @param isSucess (是否成功)
	 */
	public SaleBuildLog(    
		int id,
        		Date fromDate,
        		Date toDate,
        		Date buildTime,
        		String isSucess
        ) {
		super();  
		this.id = id;
		this.fromDate = fromDate;
		this.toDate = toDate;
		this.buildTime = buildTime;
		this.isSucess = isSucess;
	}
    
	/**
	 * 
	 * @param fromDate (开始日期)
	 * @param toDate (结束日期)
	 * @param buildTime (截数日期时间)
	 * @param isSucess (是否成功)
	 */
	public SaleBuildLog(    
		Date fromDate,
        		Date toDate,
        		Date buildTime,
        		String isSucess
        ) {
		super();		
		this.fromDate = fromDate;
		this.toDate = toDate;
		this.buildTime = buildTime;
		this.isSucess = isSucess;
	}
    

	//以上由代码生成器生成
	//以下非代码生成器生成,不可覆盖,以下是手工代码
    
}
